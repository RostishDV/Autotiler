Перед использованием:
1. Установить python 3.9.0 +
2. Скопировать файл resources\config_template.txt убрав из названия _template
3. в полученом файле resources\config.txt заменить необходимы поля.
    "name:"  отвечает за название файла excel в папке resources
    "server" название машины на которой развернута бд
    "db" название бд
    "is_zno" принимает значения true или flase в зависимости от типа плитки (Запрос на обслуживание или инцидент соответственно)
4. Выполнить команду 
    py -m pip install pyodbc
5. Выполнить команду 
    py -m pip install openpyxl

Для использования программы:
1. Скачать выгрузку
1(2). Если она в формате xls то в main.py проставить is_new = False и с помощью Excel ковертировать в формат xlsx
2. Разместить файл выгрузки в папке resources 
3. Скопировать название файла (вместе с расширением) и вставить как значение переменной name
4. Выполнить программу

В результате работы программы будут получены 2 файла scripts, представляющий отсортированные по полю от которого зависят скрипты, скрипты. И файл БП, в котором содержатся отсортированные по полям политики
